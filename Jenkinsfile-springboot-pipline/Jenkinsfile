pipeline {
    agent any
    
    tools {
        maven 'M3'
        jdk 'jdk17'
    }
    
    environment {
        APP_NAME = "springboot-app"
        PROJECT_DIR = "springbootapp"
    }
    
    options {
        timeout(time: 30, unit: 'MINUTES')
        buildDiscarder(logRotator(numToKeepStr: '10'))
        disableConcurrentBuilds()
    }
    
    stages {
        stage('Checkout') {
            steps {
                script {
                    echo '🚀 Clonage du dépôt...'
                    git branch: 'main',
                         url: 'https://github.com/ornellamabin/jenkins-mini-projet.git',
                         credentialsId: 'github-credentials',
                         extensions: [
                             [$class: 'CloneOption', 
                              depth: 1, 
                              shallow: true,
                              noTags: false,
                              timeout: 60]
                         ]
                }
            }
        }
        
        stage('Build') {
            steps {
                script {
                    echo '🔨 Construction du projet...'
                    dir(PROJECT_DIR) {
                        sh 'mvn clean compile'
                    }
                }
            }
        }
        
        stage('Test') {
            steps {
                script {
                    echo '🧪 Exécution des tests...'
                    dir(PROJECT_DIR) {
                        sh 'mvn test'
                    }
                }
            }
            
            post {
                always {
                    dir(PROJECT_DIR) {
                        junit 'target/surefire-reports/*.xml'
                    }
                }
            }
        }
        
        stage('Package') {
            steps {
                script {
                    echo '📦 Packaging de l application...'
                    dir(PROJECT_DIR) {
                        sh 'mvn package -DskipTests'
                    }
                }
            }
        }
        
        stage('Deploy') {
            steps {
                script {
                    echo '🚀 Déploiement...'
                    dir(PROJECT_DIR) {
                        echo 'Déploiement réussi!'
                        archiveArtifacts artifacts: 'target/*.jar', fingerprint: true
                        
                        // Optionnel : Stockage des métadonnées du build
                        script {
                            currentBuild.description = "Version: ${APP_NAME} - Build #${BUILD_NUMBER}"
                        }
                    }
                }
            }
        }
    }
    
    post {
        always {
            echo '🧹 Nettoyage de l espace de travail...'
            cleanWs()
            
            // Rapport de qualité du code (optionnel)
            dir(PROJECT_DIR) {
                recordIssues(
                    tools: [java(), maven()],
                    enabledForFailure: true
                )
            }
        }
        success {
            echo '✅ Pipeline exécuté avec succès!'
            emailext (
                subject: "SUCCÈS: Pipeline ${APP_NAME} - Build #${BUILD_NUMBER}",
                body: "Le build ${BUILD_NUMBER} a réussi. Consultez: ${BUILD_URL}",
                to: "votre-email@example.com"
            )
        }
        failure {
            echo '❌ Échec du pipeline!'
            emailext (
                subject: "ÉCHEC: Pipeline ${APP_NAME} - Build #${BUILD_NUMBER}",
                body: "Le build ${BUILD_NUMBER} a échoué. Consultez: ${BUILD_URL}",
                to: "votre-email@example.com"
            )
        }
        unstable {
            echo '⚠️  Pipeline instable (tests échoués)!'
        }
    }
}