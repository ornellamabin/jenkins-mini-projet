pipeline {
    agent any
    
    tools {
        maven 'M3'
        jdk 'jdk17'
    }
    
    environment {
        // Variables d'environnement simples
        SLACK_CHANNEL = '#general'  // Canal par défaut
    }
    
    options {
        timeout(time: 30, unit: 'MINUTES')
    }
    
    stages {
        stage('Checkout') {
            steps {
                script {
                    echo '🚀 Clonage du dépôt...'
                    git branch: "main",
                         url: 'https://github.com/ornellamabin/jenkins-mini-projet.git',
                         credentialsId: 'github-credentials',
                         extensions: [[$class: 'CloneOption', depth: 1, shallow: true]]
                }
            }
        }
        
        stage('Build') {
            steps {
                script {
                    echo '🔨 Construction du projet...'
                    dir('springbootapp') {
                        sh 'mvn clean compile'
                    }
                }
            }
        }
        
        stage('Test') {
            steps {
                script {
                    echo '🧪 Exécution des tests...'
                    dir('springbootapp') {
                        sh 'mvn test'
                    }
                }
            }
            post {
                always {
                    junit 'springbootapp/target/surefire-reports/*.xml'
                }
            }
        }
        
        stage('Package') {
            steps {
                script {
                    echo '📦 Packaging de l application...'
                    dir('springbootapp') {
                        sh 'mvn package -DskipTests'
                    }
                }
            }
        }
        
        stage('Deploy') {
            steps {
                script {
                    echo '🚀 Déploiement...'
                    dir('springbootapp') {
                        echo 'Déploiement réussi!'
                        archiveArtifacts artifacts: 'target/*.jar', fingerprint: true
                    }
                }
            }
        }
    }
    
    post {
        always {
            echo '🧹 Nettoyage de l espace de travail...'
        }
        success {
            echo '✅ Pipeline exécuté avec succès!'
            // Décommentez quand Slack sera configuré :
            // slackSend(channel: "${env.SLACK_CHANNEL}", message: "✅ SUCCÈS: ${env.JOB_NAME} #${env.BUILD_NUMBER}")
        }
        failure {
            echo '❌ Échec du pipeline!'
            // Décommentez quand Slack sera configuré :
            // slackSend(channel: "${env.SLACK_CHANNEL}", message: "❌ ÉCHEC: ${env.JOB_NAME} #${env.BUILD_NUMBER}")
        }
    }
}