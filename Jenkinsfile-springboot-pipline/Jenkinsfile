pipeline {
    agent any
    
    tools {
        maven 'M3'
        jdk 'jdk17'
    }
    
    options {
        timeout(time: 30, unit: 'MINUTES')
    }
    
    stages {
        stage('Checkout') {
            steps {
                script {
                    echo '🚀 Clonage du dépôt...'
                    git branch: 'main',
                         url: 'https://github.com/ornellamabin/jenkins-mini-projet.git',
                         credentialsId: 'github-credentials',
                         extensions: [
                             [$class: 'CloneOption', 
                              depth: 1, 
                              shallow: true,
                              noTags: false,
                              timeout: 60]
                         ]
                }
            }
        }
        
        stage('Explore Directory') {
            steps {
                script {
                    echo '📁 Exploration des dossiers...'
                    sh 'ls -la'  // Voir la structure
                    sh 'find . -name "pom.xml" -type f'  // Trouver le POM
                }
            }
        }
        
        stage('Build') {
            steps {
                script {
                    echo '🔨 Construction du projet...'
                    // Se déplacer dans le dossier springboot-tapp
                    dir('springboot-tapp') {
                        sh 'mvn clean compile'
                    }
                }
            }
        }
        
        stage('Test') {
            steps {
                script {
                    echo '🧪 Exécution des tests...'
                    dir('springboot-tapp') {
                        sh 'mvn test'
                    }
                }
            }
        }
        
        stage('Package') {
            steps {
                script {
                    echo '📦 Packaging de l application...'
                    dir('springboot-tapp') {
                        sh 'mvn package -DskipTests'
                    }
                }
            }
        }
        
        stage('Deploy') {
            steps {
                script {
                    echo '🚀 Déploiement...'
                    dir('springboot-tapp') {
                        echo 'Déploiement réussi!'
                        archiveArtifacts artifacts: 'target/*.jar', fingerprint: true
                    }
                }
            }
        }
    }
    
    post {
        always {
            echo '🧹 Nettoyage...'
            cleanWs()
        }
        success {
            echo '✅ Pipeline exécuté avec succès!'
        }
        failure {
            echo '❌ Échec du pipeline!'
        }
    }
}